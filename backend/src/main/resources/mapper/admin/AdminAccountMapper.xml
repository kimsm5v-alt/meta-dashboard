<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vs.meta.admin.mapper.AdminAccountMapper">

    <resultMap id="adminAccountResultMap" type="com.vs.meta.domain.AdminAccount">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="status" column="status"/>
        <result property="lastLoginAt" column="last_login_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByEmail" resultMap="adminAccountResultMap">
        /* AdminAccountMapper.findByEmail */
        SELECT id, email, password, nickname, status, last_login_at, created_at, updated_at
        FROM admin_account
        WHERE email = #{email} AND status = 'ACTIVE'
    </select>

    <select id="findById" resultMap="adminAccountResultMap">
        /* AdminAccountMapper.findById */
        SELECT id, email, password, nickname, status, last_login_at, created_at, updated_at
        FROM admin_account
        WHERE id = #{id}
    </select>

    <update id="updateLastLoginAt">
        /* AdminAccountMapper.updateLastLoginAt */
        UPDATE admin_account
        SET last_login_at = NOW(), updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        /* AdminAccountMapper.updatePassword */
        UPDATE admin_account
        SET password = #{password}, updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
