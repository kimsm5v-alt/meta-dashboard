{
  "name": "backend",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "backend/src",
  "targets": {
    "build": {
      "command": "./gradlew :backend:build"
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"const cp = require('child_process'); const cmd = process.platform === 'win32' ? 'gradlew.bat' : './gradlew'; cp.spawn(cmd, ['bootRun'], { stdio: 'inherit', shell: true });\""
      }
    }
  }
}
