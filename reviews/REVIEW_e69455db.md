> [!IMPORTANT]
> **⚠️ 수동 검토 권장 (자가힐링 주의)**
>
> AI 자가힐링 결과물이 실제 의도와 다를 수 있으므로, **상세 리뷰 내용에 기반한 수동 점검**을 우선해 주시기 바랍니다. 자가힐링 기능을 활용하실 경우 최종 결과물을 신중하게 확인해 주세요.

# 코드 리뷰 - e69455db

## 코드 복잡도 분석

**분석된 파일**: 2개 / 변경된 파일: 2개


### 정상 범위 (NONE)


**`useapidata.ts`** (other)

- 평균 복잡도: **0.001**

- 최대 복잡도: 0.008

- 청크 수: 31개


**권장사항:**

- 파일 크기가 큼 (31개 청크) - 파일 분리 검토


**`joincodemodal.tsx`** (other)

- 평균 복잡도: **0.000**

- 최대 복잡도: 0.014

- 청크 수: 72개


**권장사항:**

- 파일 크기가 큼 (72개 청크) - 파일 분리 검토


---


## [STATUS] 심각도별 이슈 요약

### Critical (즉시 수정 필요)
- **타입 불일치로 인한 런타임 오류 위험**: `ExamPeriodStatus` 타입과 실제 할당되는 객체 구조가 일치하지 않음

### High (우선 수정 권장)
- **정의되지 않은 스타일 컴포넌트 참조 가능성**: 삭제된 스타일 컴포넌트가 다른 파일에서 참조되고 있을 수 있음

### Medium (개선 권장)
- **하드코딩된 문자열 대체 불완전**: `'진행중'`, `'시작전'` 문자열이 상수화되지 않음
- **API 호출 변경에 따른 에러 처리 부재**: `fetchTeacherExams`에서 `dgnssService.getDgnssList`로 변경 시 에러 처리 흐름 검증 필요

### Low (참고 사항)
- **불필요한 코드 정리**: 사용되지 않는 스타일 컴포넌트 삭제는 긍정적이나 참조 여부 확인 필요

## 변경사항 요약
이 커밋은 젠킨스 빌드 에러 수정을 목표로 두 파일을 수정했습니다:
1. **useApiData.ts**: 학교 레벨 매핑을 상수화하고 검사 상태 타입을 객체 형식으로 변경
2. **JoinCodeModal.tsx**: 사용되지 않는 스타일 컴포넌트 7개 삭제

## 파일별 상세 분석

### frontend/src/features/api/useApiData.ts
**변경 내용:**
1. `SCHOOL_LEVEL_MAP` import 추가 및 하드코딩된 매핑 대체
2. `fetchTeacherExams` → `dgnssService.getDgnssList` 호출 방식 변경
3. `examStatus` 할당 값을 문자열에서 객체로 변경

**[PROBLEM] 발견된 문제:**
1. **[타입 불일치]**: `examStatus` 타입과 실제 할당 객체 구조 불일치
   - **위치 (라인 번호)**: 라인 453
   - **기존 코드**: `examStatus: 'in-progress',`
   - **변경된 코드**: `examStatus: { round1: '진행중', round2: '시작전' },`
   - **위험도**: Critical
   - **영향**: `ClassStats` 인터페이스의 `examStatus` 타입(`ExamPeriodStatus`)과 할당된 객체 구조가 일치하지 않으면 런타임 타입 오류 발생 가능. 관련 컴포넌트들이 `examStatus`를 문자열로 처리하도록 코딩되었다면 UI 깨짐 발생.

2. **[하드코딩된 문자열]**: 상태 문자열 상수화 미비
   - **위치 (라인 번호)**: 라인 453
   - **기존 코드**: `examStatus: 'in-progress'` (문자열)
   - **변경된 코드**: `examStatus: { round1: '진행중', round2: '시작전' }` (객체)
   - **위험도**: Medium
   - **영향**: '진행중', '시작전' 문자열이 하드코딩되어 유지보수 어려움. 오타 발생 시 디버깅 어려움.

**[GOOD] 잘된 점:**
- `SCHOOL_LEVEL_MAP` 사용으로 학교 레벨 매핑 중앙화: 유지보수성 향상
- `dgnssService.getDgnssList`로 API 호출 통일화: 일관성 향상

### frontend/src/features/groups/ui/JoinCodeModal.tsx
**변경 내용:**
7개의 사용되지 않는 스타일 컴포넌트 삭제:
- `GroupOwnerInfo`
- `InputContainer`
- `SmallLabel`
- `OptionalText`
- `NumberInput`
- `HelperText`

**[PROBLEM] 발견된 문제:**
1. **[참조 검증 부재]**: 삭제된 컴포넌트의 참조 여부 확인 필요
   - **위험도**: High
   - **영향**: 다른 파일이나 컴포넌트에서 이 스타일 컴포넌트들을 import하여 사용 중이라면 컴파일 에러 발생. 현재 모달 내에서는 사용되지 않지만, 모듈 export 여부 확인 필요.

**[GOOD] 잘된 점:**
- 불필요한 코드 제거: 번들 크기 감소 및 가독성 향상

## 보안 분석
**발견된 보안 취약점:**
해당 커밋에서는 보안과 직접 관련된 변경사항이 없습니다.

**보안 체크리스트:**
- [ ] 인증/인가 검증: 변경 없음
- [ ] 입력 검증 및 Sanitization: 변경 없음
- [ ] 민감 정보 보호: 변경 없음
- [ ] HTTPS/암호화 사용: 변경 없음

## 버그 가능성 분석
**잠재적 버그:**
1. **타입 불일치 런타임 오류**
   - **재현 조건**: `useTeacherClasses` 훅이 반환한 `examStatus` 객체를 문자열로 기대하는 컴포넌트에서 사용
   - **예상 결과**: `TypeError: Cannot read properties of undefined` 또는 UI 렌더링 오류
   - **수정 방법**: `ExamPeriodStatus` 타입 정의 확인 후 적절한 객체 구조로 수정

2. **API 호출 실패 시 fallback 동작**
   - **재현 조건**: `dgnssService.getDgnssList` 호출 실패 시
   - **예상 결과**: `useClassStudents` 훅에서 에러 처리되지 않고 빈 데이터 반환
   - **수정 방법**: 에러 발생 시 DataContext fallback 동작 보장

**Edge Case 검증:**
- [x] Null/Undefined 처리: `claId`가 없을 경우 처리 포함
- [ ] 빈 배열/객체 처리: `exams.find` 결과가 없을 경우 처리
- [ ] 경계값 (0, 음수, 최대값): `grade` 파싱 시 NaN 처리
- [ ] 동시성 문제: 다중 호출 시 상태 관리

## 성능 분석
**성능 이슈:**
해당 커밋에서는 성능에 큰 영향을 미치는 변경사항이 없습니다.

**성능 체크리스트:**
- [x] 불필요한 연산 제거: 불필요 스타일 컴포넌트 삭제
- [ ] 캐싱 활용: 변경 없음
- [ ] 비동기 처리: 변경 없음
- [ ] 메모리 효율성: 변경 없음

## 코드 품질 평가
- **가독성**: 7/10 - `SCHOOL_LEVEL_MAP` 사용으로 가독성 향상, 그러나 새로운 객체 구조 가독성 저하
- **유지보수성**: 6/10 - 타입 불일치로 인해 유지보수 어려움 증가
- **테스트 커버리지**: 평가 불가 - 변경 사항에 대한 테스트 존재 여부 확인 불가
- **문서화**: 4/10 - 변경 이유나 새로운 객체 구조에 대한 주석 부재

---

## 개선 제안 (우선순위별)

### 필수 (Must Fix)
1. **`ExamPeriodStatus` 타입과의 일관성 검증**: `shared/types`에서 `ExamPeriodStatus` 타입 정의 확인 후 일치하는 객체 구조로 수정
2. **삭제된 스타일 컴포넌트 참조 검증**: 프로젝트 전체에서 삭제된 7개 컴포넌트 참조 여부 확인

### 권장 (Should Fix)
1. **상태 문자열 상수화**: `'진행중'`, `'시작전'`을 `EXAM_STATUS` 상수 객체로 정의
2. **API 호출 에러 처리 강화**: `dgnssService.getDgnssList` 실패 시 DataContext fallback 동작 보장

### 선택 (Nice to Have)
1. **타입 안전성 개선**: `useTeacherClasses` 반환 타입에 `examStatus` 객체 구조 명시
2. **주석 추가**: 변경 이유와 새로운 객체 구조 설명 주석 추가

---

## 최종 평가

**종합 점수**: 60/100

**결론**: 
- [ ] [OK] 승인 (Approved) - 문제 없음
- [ ] [WARN] 조건부 승인 (Approved with Comments) - 경미한 이슈만 존재
- [x] [FIX] 수정 필요 (Changes Requested) - 중요 이슈 수정 후 재검토
- [ ] [REJECT] 거부 (Rejected) - 치명적 이슈로 인한 병합 불가

**핵심 코멘트:**
이 커밋의 주요 목적인 "젠킨스 빌드 에러 수정"은 `SCHOOL_LEVEL_MAP` 사용과 불필요 코드 제거로 부분적으로 달성되었습니다. 그러나 `examStatus` 타입 변경으로 인해 새로운 런타임 오류 위험을 도입했습니다. `ExamPeriodStatus` 타입 정의와의 일관성을 반드시 검증해야 하며, 삭제된 스타일 컴포넌트의 참조 여부도 확인해야 합니다. 현재 상태로는 빌드 에러가 해결되었더라도 런타임에서의 안정성을 보장할 수 없습니다.

**리뷰어 노트:**
- 검토 시간: 약 25분
- 우선 수정 항목:
  1. `ExamPeriodStatus` 타입과의 구조 일치성 검증
  2. 삭제된 스타일 컴포넌트 참조 검증
  3. 상태 문자열 상수화